import { useEffect, useState } from 'react'; import { useForm } from 'react-hook-form'; import { EmptyState } from '../../components/EmptyState/EmptyState'; import { ErrorState } from '../../components/ErrorState/ErrorState'; import { FormInput } from '../../components/FormInput/FormInput'; import { LoadingState } from '../../components/LoadingState/LoadingState'; import { voluntarioService } from '../../services/voluntarioService'; import type { Voluntario } from '../../types';
export function VoluntariosPage(){const [items,setItems]=useState<Voluntario[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');const {register,handleSubmit,reset,formState:{errors}}=useForm<Voluntario>();useEffect(()=>{voluntarioService.listar().then(setItems).catch(()=>setError('Erro ao carregar voluntários.')).finally(()=>setLoading(false));},[]);const onSubmit=async(data:Voluntario)=>{const c=await voluntarioService.criar(data);setItems(p=>[...p,c]);reset();};return <section className='space-y-4'><h1 className='text-2xl font-bold'>Voluntários</h1>{loading&&<LoadingState/>}{error&&<ErrorState message={error}/>} {!loading&&items.length===0&&<EmptyState message='Nenhum voluntário cadastrado.'/>}<ul className='grid gap-2'>{items.map(i=><li key={i.id} className='rounded-xl border p-3 text-sm'>{i.nome} - {i.areaAtuacao}</li>)}</ul><form onSubmit={handleSubmit(onSubmit)} className='grid gap-3 rounded-2xl border bg-white p-4 md:grid-cols-2'><FormInput label='Nome' registration={register('nome',{required:'Campo obrigatório'})} error={errors.nome}/><FormInput label='Área de atuação' registration={register('areaAtuacao',{required:'Campo obrigatório'})} error={errors.areaAtuacao}/><FormInput label='Telefone' registration={register('telefone',{required:'Campo obrigatório'})} error={errors.telefone}/><FormInput label='E-mail' registration={register('email',{required:'Campo obrigatório'})} error={errors.email}/><button className='rounded-xl bg-primary px-4 py-2 text-sm font-semibold text-white md:col-span-2'>Criar novo registro</button></form></section>}
