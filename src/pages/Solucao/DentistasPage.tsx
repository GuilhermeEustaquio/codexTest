import { useEffect, useState } from 'react'; import { useForm } from 'react-hook-form'; import { EmptyState } from '../../components/EmptyState/EmptyState'; import { ErrorState } from '../../components/ErrorState/ErrorState'; import { FormInput } from '../../components/FormInput/FormInput'; import { LoadingState } from '../../components/LoadingState/LoadingState'; import { dentistaService } from '../../services/dentistaService'; import type { Dentista } from '../../types';
export function DentistasPage(){const [items,setItems]=useState<Dentista[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');const {register,handleSubmit,reset,formState:{errors}}=useForm<Dentista>();useEffect(()=>{dentistaService.listar().then(setItems).catch(()=>setError('Erro ao carregar dentistas.')).finally(()=>setLoading(false));},[]);const onSubmit=async(data:Dentista)=>{const c=await dentistaService.criar(data);setItems(p=>[...p,c]);reset();};return <section className='space-y-4'><h1 className='text-2xl font-bold'>Dentistas</h1>{loading&&<LoadingState/>}{error&&<ErrorState message={error}/>} {!loading&&items.length===0&&<EmptyState message='Nenhum dentista cadastrado.'/>}<ul className='grid gap-2'>{items.map(i=><li key={i.id} className='rounded-xl border p-3 text-sm'>{i.nome} - {i.especialidade}</li>)}</ul><form onSubmit={handleSubmit(onSubmit)} className='grid gap-3 rounded-2xl border bg-white p-4 md:grid-cols-2'><FormInput label='Nome' registration={register('nome',{required:'Campo obrigatório'})} error={errors.nome}/><FormInput label='CRO' registration={register('cro',{required:'Campo obrigatório'})} error={errors.cro}/><FormInput label='Especialidade' registration={register('especialidade',{required:'Campo obrigatório'})} error={errors.especialidade}/><FormInput label='Telefone' registration={register('telefone',{required:'Campo obrigatório'})} error={errors.telefone}/><FormInput label='E-mail' registration={register('email',{required:'Campo obrigatório'})} error={errors.email}/><button className='rounded-xl bg-primary px-4 py-2 text-sm font-semibold text-white md:col-span-2'>Criar novo registro</button></form></section>}
